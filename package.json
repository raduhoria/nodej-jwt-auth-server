{
  "name": "auth-jwt",
  "version": "1.0.0",
  "description": "Express/MySQL JWT authentication service with refresh tokens and health checks",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/routes.js && node --check src/db.js && node --check src/terminate.js && node --check test/test.js && node --check scripts/seed-test-user.js && node --check scripts/benchmark-login.js && node --check spec/routes.test.js && node --check spec/mysql-live.test.js",
    "test": "npm run check && node --test spec/routes.test.js",
    "test:mysql": "node --test spec/mysql-live.test.js",
    "benchmark:login": "node scripts/benchmark-login.js",
    "seed:test": "node scripts/seed-test-user.js"
  },
  "keywords": [
    "node",
    "express",
    "jwt",
    "authentication",
    "mysql",
    "refresh-token"
  ],
  "engines": {
    "node": ">=20"
  },
  "author": "Radu Horia",
  "license": "ISC",
  "dependencies": {
    "@rodrigogs/mysql-events": "^0.6.0",
    "axios": "^1.18.1",
    "bcrypt": "^6.0.0",
    "config": "^4.4.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.22.5",
    "portscanner": "^2.2.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
